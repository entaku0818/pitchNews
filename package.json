{
  "name": "sample-http",
  "version": "0.0.1",
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.1",
    "node-fetch": "^2.6.1",
    "puppeteer": "^19.7.5"
  },
  "scripts": {
    "start": "npx functions-framework --target=scrapeGoogle --signature-type=http",
    "test": "npx functions-framework --target=addNewData --signature-type=http"

  }
}
